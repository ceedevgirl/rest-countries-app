import { ThemeClassDirective } from './theme-class.directive';

describe('ThemeClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeClassDirective();
    expect(directive).toBeTruthy();
  });
});
